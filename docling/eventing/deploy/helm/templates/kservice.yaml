apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: {{ .Values.kservice.name }}
spec:
  template:
    spec:
      containers:
        - image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          ports:
            - containerPort: {{ .Values.kservice.port }}
